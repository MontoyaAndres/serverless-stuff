name: appsyncgithubnode
schema: schema.api.graphql
authenticationType: API_KEY
mappingTemplatesLocation: mapping-templates
mappingTemplates:
  - type: Query
    field: getRepo
    dataSource: githubTable

  - type: Mutation
    field: createRepo
    dataSource: githubTable

dataSources:
  - type: AMAZON_DYNAMODB
    name: githubTable
    config:
      tableName: !Ref GitHubTable
